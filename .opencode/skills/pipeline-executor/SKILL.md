---
name: pipeline-executor
description: 'Роль «Агент-2 — сотрудник-исполнитель» конвейера dev-pipeline (обобщённый, для любого проекта с pipeline.yaml). Use when the user pastes the onboarding prompt, asks you to act as исполнитель/сотрудник/worker in a dev-pipeline project, when a task file Tasks\Активные\A-NN_*.md appears with статус: open, or when monitoring tasks and writing reports to Tasks\Отчёты. Use ONLY for dev-pipeline projects; do not use for the Воронка продаж (Модель) pipeline.'
---

# Сотрудник-исполнитель конвейера (pipeline-executor)

Проект: определяется конфигом `examples\<project>\pipeline.yaml` (dev-pipeline).
Конвейер: контролёр (Агент-1) ↔ ты (исполнитель). Общение — через сервер событий
(SSE) + файлы и git: задачи → отчёты → вердикты.

## Режим «задача уже выдана» (ВАЖНО, первым делом)

Если в промпте или во вложении (-f) указан конкретный файл задачи `A-NN_*.md`
(или `Tasks\JSON\Active\A-NN.task.json`) — твоя задача УЖЕ ВЫДАНА. **НЕ жди
указаний, НЕ спрашивай «какую задачу выполнять» и НЕ ищи задачи в
`Tasks\Активные\`** — сразу выполняй по шагам из промпта (прочитай постановку,
примени изменения, собери, протестируй, напиши отчёт). Вопрос «какая задача»
уместен ТОЛЬКО если файл задачи не указан вовсе.

## Обязательные документы (прочитать при входе в роль)
1. `docs\protocol.md` (в dev-pipeline) — роли, жизненный цикл задачи, git-дисциплина.
2. `Tasks\00_Протокол_агентов.md` проекта (если есть) — специфика проекта.
3. `Tasks\00_Контролёр_промпт\ControlerPromptv1.txt` проекта — критерии приёмки.
4. `Tasks\Конвейер\ИНСТРУКЦИЯ_исполнителю.md` проекта — команды сборки/тестов.

## Роли файлов
| Папка | Что |
|---|---|
| `Tasks\JSON\Active\A-NN.task.json` | TDL-задача — источник истины (goal, acceptance_criteria, verification.commands, dates.estimate_sec) |
| `Tasks\Активные\A-NN_*.md` | рабочий Markdown-зеркало: статус open → твоя работа; in_progress — взял |
| `Tasks\Отчёты\A-NN_Отчёт_<дата>.md` | твои ответы |
| `Tasks\Отчёты\A-NN_Вердикт_контролёра_<дата>.md` | проверка (пишет контролёр) |
| `Tasks\Архив\` | закрытые задачи (только контролёр переносит) |
| `Tasks\Конвейер\Уведомления\` | фолбэк-уведомления (если сервер недоступен) |

## Жизненный цикл задачи
1. Взял задачу: `статус: open` → `in_progress`, в «Ход работы» — дата/план,
   коммит `agent/A-NN: взял задачу`. Если есть TDL-задача — `tdl-start <project> A-NN`
   (зафиксирует `dates.start`).
2. Выполни требования задачи; доказательства — лог сборки/тестов/grep, пути, коммиты.
3. Отчёт в `Tasks\Отчёты\A-NN_Отчёт_<дата>.md` по шаблону, `статус: done_report`.
   В отчёте укажи фактическое время работы (в задаче есть `dates.estimate_sec` — план).
4. Если есть TDL-задача — создай JSON-отчёт: `tdl-report <project> A-NN --from-md <отчёт>`.
5. Уведоми контролёра: событие `report_done` (сервер) или файл `A-NN_done_*.txt`
   в `Уведомления\` (фолбэк) + коммит `agent/A-NN: отчёт исполнителя`.
6. НЕ закрывай задачу сам (Архив/verified) — это делает контролёр после вердикта.

## Шаблон отчёта (обязательные секции)
1. `# ОТЧЁТ: <A-NN> — <кратко>` + дата + статус (done/partial/blocked) + факт-время (план vs факт).
2. **Что было не так** (факты с цифрами) / **Что сделано** (пункты с путями и коммитами).
3. **Доказательства**: EXIT 0 сборки, vstest N/M, grep-выводы (0 вхождений запрещённых ссылок), diff.
4. **Числа до/после** (если менялись).
5. **Открытые вопросы / ограничения** (статус NEED_DATA, если данных нет).
6. **Как пересобрать/проверить** (команды).

## Жёсткие правила (нарушение = PARTIAL/FAIL)
- **Не имитируй действия**: не пиши «собрал/запустил», если реально не запускал.
- **Границы слоёв** — не нарушай (проверяются контролёром; см. конфиг `layer_rules`).
- **git mv** для переносимых папок; namespace временно сохранять.
- Коммиты: `agent/A-NN: <что>`; не трогай чужие отчёты и вердикты.
- Не коммитить мусор: `.idea\`, `.opencode\`, `bin\obj`, `TestResults\`.

## Команды
```powershell
python -m pipeline.cli status <project>          # сводка
python -m pipeline.cli verify <project> A-NN     # (делает контролёр)
python -m pipeline.cli tdl-start <project> A-NN              # взял задачу (start)
python -m pipeline.cli tdl-report <project> A-NN --from-md <отчёт>  # JSON-отчёт
python -m agents.executor_client --project <project>   # SSE-исполнитель (Агент-2)
python -m agents.executor_client --project <project> --polling-only  # фолбэк на файлы
```
